# 게시판 스타일 UI 리디자인 계획

## 개요

Jekyll 블로그의 메인 페이지와 카테고리 페이지를 게시판(Bulletin Board) 스타일로 리디자인합니다.

---

## 1. 현재 상태

### 메인 페이지 (`index.html`)
- `layout: home` 사용
- 최근 글 7개 + 페이지네이션만 표시
- 카테고리 구조 파악 어려움

### 카테고리 페이지 (`*/index.html`)
- `layout: archive-subtoc` 사용
- 하위 카테고리 목록 또는 포스트 목록 표시
- 인기 글 기능 없음

### 포스트 front matter
- `pin` 속성 미사용

---

## 2. 목표 디자인

### 2.1 메인 페이지

```
┌─────────────────────────────────────────────────────────────┐
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📊 ALGORITHM                            [더보기 →]  │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ 📌 인기 글                                          │   │
│  │ ├─ [이론] 트라이(Trie)의 원리와 구현     2026-01-03 │   │
│  │ └─ [이론] 동적 프로그래밍 완벽 가이드    2025-10-25 │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ 최신 글                                             │   │
│  │ ├─ [이론] 위상 정렬의 원리와 구현        2026-01-03 │   │
│  │ ├─ [BOJ]  1234번 - 문제 제목             2026-01-02 │   │
│  │ └─ [이론] 동전 교환 문제                 2026-01-03 │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  (다른 카테고리들도 동일한 형식으로 반복)                   │
└─────────────────────────────────────────────────────────────┘
```

**특징:**
- 각 카테고리가 독립된 게시판 섹션으로 표시
- 인기 글(pin: true)이 상단에 고정
- 하위 분류가 `[이론]`, `[BOJ]` 형태로 제목 앞에 표시
- 각 카테고리당 최신 글 3~5개 표시
- "더보기" 링크로 카테고리 페이지 이동

### 2.2 카테고리 페이지

```
┌─────────────────────────────────────────────────────────────┐
│  홈 > ALGORITHM                                             │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📊 ALGORITHM                          총 2,434 글   │   │
│  │ 알고리듬 이론과 백준(BOJ) 문제 풀이를 다룹니다      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📂 하위 분류                                        │   │
│  │   [이론] 56 글    |    [BOJ] 2,378 글               │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📌 인기 글                                          │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ [이론] 트라이(Trie)의 원리와 구현        2026-01-03 │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📋 전체 글                                          │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │  [이론]  위상 정렬의 원리와 구현         2026-01-03 │   │
│  │  [BOJ]   5678번 - 문제 제목              2026-01-02 │   │
│  │  ...                                                │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│                    < 1  2  3  ... >                         │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 하위 분류 페이지

- 카테고리 페이지와 동일한 구조
- 단, 하위 분류 탭 없음 (이미 최하위이므로)
- 분류 라벨 생략 (모두 같은 분류이므로)

---

## 3. 파일 구조

### 3.1 생성할 파일

| 파일 | 용도 |
|------|------|
| `_layouts/home-board.html` | 메인 페이지 게시판 레이아웃 |
| `_layouts/category-board.html` | 카테고리 페이지 게시판 레이아웃 |
| `_includes/board/section.html` | 게시판 섹션 컴포넌트 |
| `_includes/board/header.html` | 카테고리 헤더 컴포넌트 |
| `_includes/board/subcategory-tabs.html` | 하위 분류 탭 컴포넌트 |
| `_includes/board/pinned-posts.html` | 인기 글 목록 컴포넌트 |
| `_includes/board/post-list.html` | 일반 글 목록 컴포넌트 |
| `_includes/board/post-row.html` | 글 한 줄 컴포넌트 |

### 3.2 수정할 파일

| 파일 | 변경 내용 |
|------|----------|
| `index.html` | `layout: home-board`로 변경 |
| `assets/css/main.scss` | 게시판 스타일 추가 |
| `algorithm/index.html` | `layout: category-board`로 변경 |
| `dev/index.html` | `layout: category-board`로 변경 |
| `game-design/index.html` | `layout: category-board`로 변경 |
| `library/index.html` | `layout: category-board`로 변경 |
| `daily-life/index.html` | `layout: category-board`로 변경 |
| (하위 카테고리 index.html들) | 동일하게 변경 |

### 3.3 인기 글 설정

포스트 front matter에 `pin: true` 추가:
```yaml
---
layout: single
title: "트라이(Trie)의 원리와 구현"
pin: true
---
```

---

## 4. 구현 순서

### Phase 1: 기반 컴포넌트 생성
- [ ] 1.1 `_includes/board/post-row.html` 생성
- [ ] 1.2 `_includes/board/post-list.html` 생성
- [ ] 1.3 `_includes/board/pinned-posts.html` 생성
- [ ] 1.4 `_includes/board/section.html` 생성

### Phase 2: 메인 페이지 구현
- [ ] 2.1 `_layouts/home-board.html` 생성
- [ ] 2.2 `index.html` 레이아웃 변경
- [ ] 2.3 메인 페이지 스타일링 (`main.scss`)

### Phase 3: 카테고리 페이지 구현
- [ ] 3.1 `_includes/board/header.html` 생성
- [ ] 3.2 `_includes/board/subcategory-tabs.html` 생성
- [ ] 3.3 `_layouts/category-board.html` 생성
- [ ] 3.4 메인 카테고리 index.html들 레이아웃 변경
- [ ] 3.5 하위 카테고리 index.html들 레이아웃 변경
- [ ] 3.6 카테고리 페이지 스타일링

### Phase 4: 인기 글 설정
- [ ] 4.1 각 카테고리별 대표 포스트에 `pin: true` 추가

### Phase 5: 테스트 및 마무리
- [ ] 5.1 로컬에서 Jekyll 빌드 테스트
- [ ] 5.2 반응형 디자인 확인
- [ ] 5.3 커밋 및 푸시

---

## 5. 상세 구현 명세

### 5.1 `_includes/board/post-row.html`

**입력 변수:**
- `post`: 포스트 객체
- `show_subcategory`: 하위 분류 라벨 표시 여부 (boolean)
- `subcategory_name`: 하위 분류 이름 (optional)

**출력:**
```html
<li class="board__post-row">
  {% if show_subcategory %}
  <span class="board__subcategory">[{{ subcategory_name }}]</span>
  {% endif %}
  <a href="{{ post.url }}" class="board__post-title">{{ post.title }}</a>
  <span class="board__post-date">{{ post.date | date: "%Y-%m-%d" }}</span>
</li>
```

### 5.2 `_includes/board/pinned-posts.html`

**입력 변수:**
- `posts`: 포스트 배열
- `show_subcategory`: 하위 분류 라벨 표시 여부

**로직:**
1. `posts`에서 `pin: true`인 포스트 필터링
2. 있으면 "📌 인기 글" 섹션 표시
3. 없으면 아무것도 표시하지 않음

### 5.3 `_includes/board/section.html`

**입력 변수:**
- `category_name`: 카테고리 이름
- `category_url`: 카테고리 링크
- `category_icon`: 아이콘 (emoji)
- `posts`: 해당 카테고리 포스트 배열
- `max_posts`: 표시할 최대 포스트 수 (기본값: 5)
- `show_subcategory`: 하위 분류 표시 여부

**구조:**
```html
<section class="board__section">
  <div class="board__section-header">
    <h2>{{ category_icon }} {{ category_name }}</h2>
    <a href="{{ category_url }}">더보기 →</a>
  </div>

  {% include board/pinned-posts.html posts=posts show_subcategory=show_subcategory %}

  <div class="board__recent">
    <h3>최신 글</h3>
    {% include board/post-list.html posts=posts limit=max_posts show_subcategory=show_subcategory %}
  </div>
</section>
```

### 5.4 `_layouts/home-board.html`

**구조:**
```html
---
layout: default
---

<div class="board-home">
  {% comment %} ALGORITHM 섹션 {% endcomment %}
  {% assign algo_posts = site.posts | where_exp: "post", "post.path contains 'algorithm/'" %}
  {% include board/section.html
     category_name="ALGORITHM"
     category_url="/algorithm/"
     category_icon="📊"
     posts=algo_posts
     max_posts=5
     show_subcategory=true %}

  {% comment %} 다른 카테고리들도 동일하게 {% endcomment %}
  ...
</div>
```

### 5.5 하위 분류 이름 추출 로직

포스트 경로에서 하위 분류를 추출:
```liquid
{% assign path_parts = post.path | split: "/" %}
{% if path_parts.size >= 3 %}
  {% assign subcategory = path_parts[1] %}
{% endif %}
```

예시:
- `algorithm/theory/_posts/...` → `theory` → `[이론]`
- `algorithm/boj/_posts/...` → `boj` → `[BOJ]`

### 5.6 하위 분류 한글 매핑

`_data/subcategory-labels.yml` 생성:
```yaml
theory: "이론"
boj: "BOJ"
project-review: "프로젝트"
played-game-review: "플레이"
consideration: "고찰"
book: "책"
lecture: "강의"
happiness: "행복"
appreciation: "감사"
design-pattern: "패턴"
object-oriented-programming: "OOP"
reactive-programming: "반응형"
graphics: "Graphics"
unity: "Unity"
language: "언어"
test-driven-development: "TDD"
```

---

## 6. CSS 스타일 명세

### 6.1 색상 팔레트 (기존 neon 테마 활용)

```scss
$board-border: rgba(255, 255, 255, 0.1);
$board-bg: rgba(0, 0, 0, 0.2);
$board-header-bg: rgba(0, 212, 255, 0.1);
$board-pinned-bg: rgba(255, 200, 0, 0.05);
$accent-cyan: #00d4ff;
$accent-green: #00ff88;
$text-primary: #fff6fb;
$text-secondary: rgba(255, 255, 255, 0.7);
```

### 6.2 주요 클래스

```scss
.board-home { }
.board__section { }
.board__section-header { }
.board__pinned { }
.board__recent { }
.board__post-row { }
.board__subcategory { }
.board__post-title { }
.board__post-date { }
.board__more-link { }

.category-board { }
.category-board__header { }
.category-board__tabs { }
.category-board__tab { }
.category-board__content { }
```

---

## 7. 카테고리 아이콘 매핑

| 카테고리 | 아이콘 |
|----------|--------|
| ALGORITHM | 📊 |
| GAME DESIGN | 🎮 |
| DEV | 💻 |
| LIBRARY | 📚 |
| DAILY LIFE | 🌸 |
| NOTE | 📝 |

---

## 8. 메인 페이지 카테고리 표시 순서

1. ALGORITHM (가장 많은 포스트)
2. GAME DESIGN
3. DEV
4. LIBRARY
5. DAILY LIFE
6. NOTE (가장 적은 포스트, 또는 숨김 처리)

---

## 9. 체크리스트

### 구현 전 확인
- [ ] 기존 레이아웃 백업 (git으로 관리되므로 OK)
- [ ] `_data/navigation.yml` 구조 파악 완료
- [ ] 카테고리별 포스트 경로 패턴 파악 완료

### 구현 후 확인
- [ ] 메인 페이지에서 모든 카테고리 섹션 표시
- [ ] 각 카테고리 섹션에 인기 글 + 최신 글 표시
- [ ] 하위 분류 라벨 정상 표시
- [ ] "더보기" 링크 정상 작동
- [ ] 카테고리 페이지 헤더 정상 표시
- [ ] 하위 분류 탭 정상 작동
- [ ] 페이지네이션 정상 작동
- [ ] 모바일 반응형 정상
- [ ] 다크 테마(neon skin) 스타일 일관성

---

## 10. 롤백 계획

문제 발생 시:
1. `git checkout HEAD~1 -- index.html _layouts/ _includes/board/`
2. 또는 커밋 단위로 `git revert`

---

*문서 작성일: 2026-01-03*
*작성자: Claude Code*
