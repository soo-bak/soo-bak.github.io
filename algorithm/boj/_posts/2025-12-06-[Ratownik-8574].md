---
layout: single
title: "[백준 8574] Ratownik (C#, C++) - soo:bak"
date: "2025-12-06 20:25:00 +0900"
description: 구명요원의 시야 반경 밖에 있는 아이들의 수를 좌표 거리로 계산하는 백준 8574번 Ratownik 문제의 C# 및 C++ 풀이와 해설
tags:
  - 백준
  - BOJ
  - 8574
  - C#
  - C++
  - 알고리즘
  - 수학
  - 기하학
  - pythagoras
keywords: "백준 8574, 백준 8574번, BOJ 8574, Ratownik, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[8574번 - Ratownik](https://www.acmicpc.net/problem/8574)

## 설명
해변에서 구명요원의 위치와 시야 반경이 주어집니다. 구명요원으로부터의 거리가 시야 반경을 넘는 아이들은 보이지 않습니다.

각 아이의 좌표가 주어질 때, 시야 밖에 있는 아이들의 수를 출력하는 문제입니다.

<br>

## 접근법
먼저, 각 아이와 구명요원 사이의 유클리드 거리 제곱을 계산합니다.

다음으로, 시야 반경의 제곱과 비교합니다. 거리 제곱이 반경 제곱보다 크면 시야 밖에 있는 것이므로 개수를 하나 늘립니다.

이렇게 제곱 비교를 사용하면 제곱근 연산 없이 정수 연산만으로 처리할 수 있습니다.

<br>

## Code

### C#

```csharp
using System;

namespace Solution {
  class Program {
    static void Main(string[] args) {
      var first = Console.ReadLine()!.Split();
      var n = int.Parse(first[0]);
      var k = int.Parse(first[1]);
      var x = int.Parse(first[2]);
      var y = int.Parse(first[3]);

      var k2 = k * k;
      var ans = 0;

      for (var i = 0; i < n; i++) {
        var line = Console.ReadLine()!.Split();
        var cx = int.Parse(line[0]);
        var cy = int.Parse(line[1]);
        var dx = x - cx;
        var dy = y - cy;
        var dist2 = dx * dx + dy * dy;
        if (dist2 > k2)
          ans++;
      }

      Console.WriteLine(ans);
    }
  }
}
```

### C++

```cpp
#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int n, k, x, y; cin >> n >> k >> x >> y;
  int k2 = k * k;
  int ans = 0;

  for (int i = 0; i < n; i++) {
    int cx, cy; cin >> cx >> cy;
    int dx = x - cx;
    int dy = y - cy;
    int dist2 = dx * dx + dy * dy;
    if (dist2 > k2)
      ans++;
  }

  cout << ans << "\n";

  return 0;
}
```
