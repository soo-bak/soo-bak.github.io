---
layout: single
title: "[백준 15688] 수 정렬하기 5 (C#, C++) - soo:bak"
date: "2025-12-07 23:30:00 +0900"
description: 최대 100만 개 정수를 비내림차순으로 정렬하는 백준 15688번 수 정렬하기 5 문제의 C# 및 C++ 풀이와 해설
---

## 문제 링크
[15688번 - 수 정렬하기 5](https://www.acmicpc.net/problem/15688)

## 설명
N개의 정수를 비내림차순으로 정렬하여 출력하는 문제입니다. 같은 수가 여러 번 중복될 수 있습니다.

<br>

## 접근법
모든 수를 배열에 저장한 뒤 기본 정렬 함수로 정렬합니다. 입력 크기가 최대 100만 개이므로 빠른 입출력을 사용해야 시간 제한을 통과할 수 있습니다.

<br>

- - -

## Code

### C#
```csharp
using System;
using System.Text;

class FastScanner {
  readonly byte[] buf = new byte[1 << 16];
  int idx, len;
  int ReadByte() {
    if (idx >= len) {
      len = Console.OpenStandardInput().Read(buf, 0, buf.Length);
      idx = 0;
      if (len == 0) return -1;
    }
    return buf[idx++];
  }
  public int NextInt() {
    int c, sign = 1, val = 0;
    do c = ReadByte(); while (c <= ' ');
    if (c == '-') { sign = -1; c = ReadByte(); }
    while (c > ' ') { val = val * 10 + (c - '0'); c = ReadByte(); }
    return val * sign;
  }
}

class Program {
  static void Main() {
    var fs = new FastScanner();
    var n = fs.NextInt();
    var arr = new int[n];
    for (var i = 0; i < n; i++) arr[i] = fs.NextInt();

    Array.Sort(arr);

    var sb = new StringBuilder();
    for (var i = 0; i < n; i++) sb.AppendLine(arr[i].ToString());
    Console.Write(sb.ToString());
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

typedef vector<int> vi;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int n; cin >> n;
  vi v(n);
  for (int i = 0; i < n; i++) cin >> v[i];

  sort(v.begin(), v.end());

  for (int x : v) cout << x << "\n";

  return 0;
}
```
