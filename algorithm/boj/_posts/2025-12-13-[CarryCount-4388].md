---
layout: single
title: "[백준 4388] 받아올림 (C#, C++) - soo:bak"
date: "2025-12-13 22:20:00 +0900"
description: 두 수를 자릿수별로 더하며 carry 발생 횟수를 세는 백준 4388번 받아올림 문제의 C#/C++ 풀이
tags:
  - 백준
  - BOJ
  - 4388
  - C#
  - C++
  - 알고리즘
  - 수학
  - 구현
  - arithmetic
keywords: "백준 4388, 백준 4388번, BOJ 4388, CarryCount, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[4388번 - 받아올림](https://www.acmicpc.net/problem/4388)

## 설명
두 수를 더할 때 받아올림이 발생하는 횟수를 구하는 문제입니다.

0 0이 입력되면 종료합니다.

<br>

## 접근법
1의 자리부터 각 자릿수를 더하며 받아올림 여부를 확인합니다.

자릿수 합과 이전 받아올림의 합이 10 이상이면 받아올림이 발생합니다.

입력이 10자리까지 가능하므로 64비트 정수를 사용합니다.

<br>

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    while (true) {
      var line = Console.ReadLine()!.Split();
      var a = long.Parse(line[0]);
      var b = long.Parse(line[1]);
      if (a == 0 && b == 0) break;

      var carry = 0;
      var cnt = 0;
      while (a > 0 || b > 0) {
        var da = (int)(a % 10);
        var db = (int)(b % 10);
        if (da + db + carry >= 10) {
          cnt++;
          carry = 1;
        } else carry = 0;
        a /= 10;
        b /= 10;
      }
      Console.WriteLine(cnt);
    }
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  while (true) {
    ll a, b;
    if (!(cin >> a >> b)) break;
    if (a == 0 && b == 0) break;

    int carry = 0, cnt = 0;
    while (a > 0 || b > 0) {
      int da = a % 10;
      int db = b % 10;
      if (da + db + carry >= 10) {
        cnt++;
        carry = 1;
      } else carry = 0;
      a /= 10;
      b /= 10;
    }
    cout << cnt << "\n";
  }

  return 0;
}
```
