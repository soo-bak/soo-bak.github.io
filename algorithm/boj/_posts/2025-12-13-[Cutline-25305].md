---
layout: single
title: "[백준 25305] 커트라인 (C#, C++) - soo:bak"
date: "2025-12-13 00:03:00 +0900"
description: 점수를 내림차순 정렬해 k번째 점수를 커트라인으로 출력하는 백준 25305번 커트라인 문제의 C# 및 C++ 풀이와 해설
---

## 문제 링크
[25305번 - 커트라인](https://www.acmicpc.net/problem/25305)

## 설명
점수가 주어질 때, 상위 k명이 상을 받는다면 커트라인 점수를 구하는 문제입니다.

<br>

## 접근법
점수를 내림차순으로 정렬합니다.

k번째 점수가 상을 받는 사람 중 가장 낮은 점수이므로 커트라인이 됩니다.

<br>

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    var nk = Console.ReadLine()!.Split();
    var n = int.Parse(nk[0]);
    var k = int.Parse(nk[1]);
    var line = Console.ReadLine()!.Split();
    var scores = new int[n];
    for (var i = 0; i < n; i++)
      scores[i] = int.Parse(line[i]);
    Array.Sort(scores, (a, b) => b.CompareTo(a));
    Console.WriteLine(scores[k - 1]);
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

typedef vector<int> vi;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int n, k; cin >> n >> k;
  vi v(n);
  for (int i = 0; i < n; i++)
    cin >> v[i];
  sort(v.begin(), v.end(), greater<int>());
  cout << v[k - 1] << "\n";

  return 0;
}
```
