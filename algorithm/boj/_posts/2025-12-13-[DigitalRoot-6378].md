---
layout: single
title: "[백준 6378] 디지털 루트 (C#, C++) - soo:bak"
date: "2025-12-13 17:01:00 +0900"
description: 큰 수 문자열의 자릿수를 반복 합산해 한 자리로 만드는 디지털 루트를 구하는 백준 6378번 문제의 C# 및 C++ 풀이와 해설
---

## 문제 링크
[6378번 - 디지털 루트](https://www.acmicpc.net/problem/6378)

## 설명
주어진 수의 자릿수를 반복해서 더해 한 자리가 될 때까지 구하는 디지털 루트 문제입니다.

<br>

## 접근법
문자열로 입력을 받아 각 자릿수를 더합니다.

결과가 10 이상이면 다시 자릿수를 더하는 과정을 반복합니다.

한 자리가 되면 그 값을 출력하고, 0이 입력되면 종료합니다.

<br>

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    while (true) {
      var s = Console.ReadLine()!;
      if (s == "0") break;
      var val = 0;
      var cur = s;
      while (true) {
        val = 0;
        foreach (var c in cur)
          val += c - '0';
        if (val < 10) break;
        cur = val.ToString();
      }
      Console.WriteLine(val);
    }
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  string s;
  while (cin >> s && s != "0") {
    string cur = s;
    int val = 0;
    while (true) {
      val = 0;
      for (char c : cur)
        val += c - '0';
      if (val < 10) break;
      cur = to_string(val);
    }
    cout << val << "\n";
  }

  return 0;
}
```
