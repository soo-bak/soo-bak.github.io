---
layout: single
title: "[백준 14065] Gorivo (C#, C++) - soo:bak"
date: "2025-12-13 05:01:00 +0900"
description: MPG를 L/100km로 변환하는 공식 235.214583 / mpg를 사용해 연비를 환산하는 백준 14065번 Gorivo 문제의 C# 및 C++ 풀이와 해설
---

## 문제 링크
[14065번 - Gorivo](https://www.acmicpc.net/problem/14065)

## 설명
연비를 mpg에서 L/100km로 환산하는 문제입니다.

<br>

## 접근법
변환 공식은 235.214583 / mpg입니다.

입력을 읽어 공식을 적용한 결과를 출력합니다.

<br>

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    const double factor = 235.214583;
    var mpg = double.Parse(Console.ReadLine()!);
    var ans = factor / mpg;
    Console.WriteLine($"{ans:F6}");
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  const double factor = 235.214583;
  double mpg; cin >> mpg;
  double ans = factor / mpg;
  cout.setf(ios::fixed);
  cout.precision(6);
  cout << ans << "\n";

  return 0;
}
```
