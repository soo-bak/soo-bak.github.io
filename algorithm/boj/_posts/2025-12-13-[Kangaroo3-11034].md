---
layout: single
title: "[백준 11034] 캥거루 세마리2 (C#, C++) - soo:bak"
date: "2025-12-13 17:01:00 +0900"
description: 세 좌표 간격 중 큰 값에서 1을 뺀 값이 최대 점프 횟수임을 이용하는 백준 11034번 캥거루 세마리2 문제의 C# 및 C++ 풀이와 해설
tags:
  - 백준
  - BOJ
  - 11034
  - C#
  - C++
  - 알고리즘
keywords: "백준 11034, 백준 11034번, BOJ 11034, Kangaroo3, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[11034번 - 캥거루 세마리2](https://www.acmicpc.net/problem/11034)

## 설명
세 마리 캥거루가 점프할 수 있는 최대 횟수를 구하는 문제입니다.

<br>

## 접근법
두 간격 중 더 큰 간격을 줄여 나가는 것이 최적입니다.

큰 간격에서 한 칸씩 채울 수 있으므로 최대 점프 횟수는 큰 간격에서 1을 뺀 값입니다.

<br>

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    var input = "";
    while ((input = Console.ReadLine()) != null) {
      var line = input.Split();
      var a = int.Parse(line[0]);
      var b = int.Parse(line[1]);
      var c = int.Parse(line[2]);
      var ans = Math.Max(b - a, c - b) - 1;
      Console.WriteLine(ans);
    }
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int a, b, c;
  while (cin >> a >> b >> c) {
    int ans = max(b - a, c - b) - 1;
    cout << ans << "\n";
  }

  return 0;
}
```
