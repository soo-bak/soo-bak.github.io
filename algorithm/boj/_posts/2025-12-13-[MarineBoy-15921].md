---
layout: single
title: "[백준 15921] 수찬은 마린보이야!! (C#, C++) - soo:bak"
date: "2025-12-13 17:01:00 +0900"
description: 평균과 기대값이 동일해 항상 1이거나 divide by zero가 되는 것을 출력하는 백준 15921번 문제의 C# 및 C++ 풀이와 해설
tags:
  - 백준
  - BOJ
  - 15921
  - C#
  - C++
  - 알고리즘
keywords: "백준 15921, 백준 15921번, BOJ 15921, MarineBoy, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[15921번 - 수찬은 마린보이야!!](https://www.acmicpc.net/problem/15921)

## 설명
평균을 기댓값으로 나눈 값을 구하는 문제입니다.

<br>

## 접근법
동일한 데이터에서 평균과 기댓값은 항상 같습니다.

따라서 데이터가 존재하면 결과는 항상 1.00입니다.

n이 0이면 데이터가 없으므로 divide by zero를 출력합니다.

<br>

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    var n = int.Parse(Console.ReadLine()!);
    if (n == 0) {
      Console.WriteLine("divide by zero");
      return;
    }
    Console.ReadLine();
    Console.WriteLine("1.00");
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int n;
  if (!(cin >> n)) return 0;
  if (n == 0) {
    cout << "divide by zero\n";
    return 0;
  }
  for (int i = 0; i < n; i++) {
    int x; cin >> x;
  }
  cout << "1.00\n";

  return 0;
}
```
