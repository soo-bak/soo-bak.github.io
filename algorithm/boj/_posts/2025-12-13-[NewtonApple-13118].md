---
layout: single
title: "[백준 13118] 뉴턴과 사과 (C#, C++) - soo:bak"
date: "2025-12-13 05:01:00 +0900"
description: 사과가 떨어질 때 x좌표가 같은 사람만 충돌하므로 사람 좌표 중 사과 중심 x와 같은 번호를 찾는 백준 13118번 뉴턴과 사과 문제의 C# 및 C++ 풀이와 해설
tags:
  - 백준
  - BOJ
  - 13118
  - C#
  - C++
  - 알고리즘
  - 구현
keywords: "백준 13118, 백준 13118번, BOJ 13118, NewtonApple, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[13118번 - 뉴턴과 사과](https://www.acmicpc.net/problem/13118)

## 설명
사과가 떨어질 때 어느 사람의 머리 위로 떨어지는지 구하는 문제입니다.

<br>

## 접근법
사과는 수직으로 떨어지므로 사과 중심의 x좌표는 변하지 않습니다.

네 사람의 x좌표 중 사과 중심의 x좌표와 같은 것이 있으면 해당 번호를 출력합니다.

일치하는 사람이 없으면 0을 출력합니다.

<br>

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    var p = Array.ConvertAll(Console.ReadLine()!.Split(), int.Parse);
    var apple = Array.ConvertAll(Console.ReadLine()!.Split(), int.Parse);
    var x = apple[0];
    var ans = 0;
    for (var i = 0; i < 4; i++)
      if (p[i] == x) { ans = i + 1; break; }
    Console.WriteLine(ans);
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int p[4];
  for (int i = 0; i < 4; i++)
    cin >> p[i];
  int x, y, r; cin >> x >> y >> r;
  int ans = 0;
  for (int i = 0; i < 4; i++)
    if (p[i] == x) { ans = i + 1; break; }
  cout << ans << "\n";

  return 0;
}
```
