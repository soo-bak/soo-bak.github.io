---
layout: single
title: "[백준 4619] 루트 (C#, C++) - soo:bak"
date: "2025-12-13 17:01:00 +0900"
description: B에 가장 가까운 A^N을 찾기 위해 A를 증가시키며 차이를 비교하는 백준 4619번 루트 문제의 C# 및 C++ 풀이와 해설
tags:
  - 백준
  - BOJ
  - 4619
  - C#
  - C++
  - 알고리즘
  - 수학
  - 브루트포스
keywords: "백준 4619, 백준 4619번, BOJ 4619, Root, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[4619번 - 루트](https://www.acmicpc.net/problem/4619)

## 설명
a의 n제곱이 b에 가장 가까운 정수 a를 구하는 문제입니다.

<br>

## 접근법
a를 1부터 증가시키며 a의 n제곱을 계산해 b와의 차이를 비교합니다.

a의 n제곱이 b를 넘으면 더 이상의 a는 거리가 증가하므로 탐색을 종료합니다.

이후, 지금까지의 최소 차이를 가진 a를 출력합니다.

<br>

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    while (true) {
      var line = Console.ReadLine()!.Split();
      var b = int.Parse(line[0]);
      var n = int.Parse(line[1]);
      if (b == 0 && n == 0) break;

      var bestA = 1;
      var bestVal = 1;
      for (var a = 1; ; a++) {
        var val = 1;
        for (var i = 0; i < n; i++)
          val *= a;
        if (Math.Abs(val - b) < Math.Abs(bestVal - b)) {
          bestA = a;
          bestVal = val;
        } else if (val > b) break;
      }
      Console.WriteLine(bestA);
    }
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  while (true) {
    int b, n;
    if (!(cin >> b >> n)) break;
    if (b == 0 && n == 0) break;

    int bestA = 1, bestVal = 1;
    for (int a = 1;; a++) {
      int val = 1;
      for (int i = 0; i < n; i++)
        val *= a;
      if (abs(val - b) < abs(bestVal - b)) {
        bestA = a;
        bestVal = val;
      } else if (val > b) break;
    }
    cout << bestA << "\n";
  }

  return 0;
}
```
