---
layout: single
title: "[백준 17350] 2루수 이름이 뭐야 (C#, C++) - soo:bak"
date: "2025-12-13 17:01:00 +0900"
description: 이름 목록에 'anj'가 있는지 여부에 따라 다른 문자열을 출력하는 백준 17350번 문제의 C# 및 C++ 풀이와 해설
tags:
  - 백준
  - BOJ
  - 17350
  - C#
  - C++
  - 알고리즘
  - 구현
  - 문자열
keywords: "백준 17350, 백준 17350번, BOJ 17350, SecondBaseman, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[17350번 - 2루수 이름이 뭐야](https://www.acmicpc.net/problem/17350)

## 설명
선수 이름 목록에서 특정 이름이 있는지 확인하는 문제입니다.

<br>

## 접근법
이름을 한 줄씩 읽어 anj와 일치하는지 검사합니다.

찾으면 해당 여부를 기록하고, 결과에 따라 다른 문자열을 출력합니다.

<br>

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    var n = int.Parse(Console.ReadLine()!);
    var found = false;
    for (var i = 0; i < n; i++) {
      var name = Console.ReadLine()!;
      if (name == "anj") found = true;
    }
    Console.WriteLine(found ? "뭐야;" : "뭐야?");
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int n;
  if (!(cin >> n)) return 0;
  bool found = false;
  for (int i = 0; i < n; i++) {
    string s; cin >> s;
    if (s == "anj") found = true;
  }
  cout << (found ? "뭐야;" : "뭐야?") << "\n";

  return 0;
}
```
