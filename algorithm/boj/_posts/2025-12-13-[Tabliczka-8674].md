---
layout: single
title: "[백준 8674] Tabliczka (C#, C++) - soo:bak"
date: "2025-12-13 13:02:00 +0900"
description: 한 번의 직선 절단으로 초콜릿을 최대한 공평하게 나눌 때 두 조각의 차이가 최소가 되는 값을 구하는 백준 8674번 Tabliczka 문제의 C# 및 C++ 풀이와 해설
tags:
  - 백준
  - BOJ
  - 8674
  - C#
  - C++
  - 알고리즘
  - 수학
keywords: "백준 8674, 백준 8674번, BOJ 8674, Tabliczka, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[8674번 - Tabliczka](https://www.acmicpc.net/problem/8674)

## 설명
초콜릿을 한 번만 잘라 두 조각의 차이가 최소가 되도록 할 때 그 차이를 구하는 문제입니다.

<br>

## 접근법
어느 한 변이라도 짝수이면 정중앙을 자를 수 있어 차이는 0입니다.

두 변 모두 홀수이면 완전히 반으로 나눌 수 없고, 두 변 중 작은 값이 최소 차이가 됩니다.

<br>

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    var line = Console.ReadLine()!.Split();
    var a = long.Parse(line[0]);
    var b = long.Parse(line[1]);

    var ans = 0L;
    if ((a % 2 == 1) && (b % 2 == 1)) ans = Math.Min(a, b);

    Console.WriteLine(ans);
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  ll a, b; cin >> a >> b;
  ll ans = 0;
  if ((a & 1LL) && (b & 1LL)) ans = min(a, b);
  cout << ans << "\n";

  return 0;
}
```
