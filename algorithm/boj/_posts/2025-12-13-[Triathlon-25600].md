---
layout: single
title: "[백준 25600] Triathlon (C#, C++) - soo:bak"
date: "2025-12-13 05:01:00 +0900"
description: 참가자들의 점수 a,d,g로 a*(d+g) 계산 후 a==(d+g)면 두 배, 그중 최대를 찾는 백준 25600번 Triathlon 문제의 C# 및 C++ 풀이와 해설
tags:
  - 백준
  - BOJ
  - 25600
  - C#
  - C++
  - 알고리즘
  - 수학
  - 구현
  - arithmetic
keywords: "백준 25600, 백준 25600번, BOJ 25600, Triathlon, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[25600번 - Triathlon](https://www.acmicpc.net/problem/25600)

## 설명
참가자들의 점수 중 최댓값을 구하는 문제입니다.

<br>

## 접근법
각 참가자의 점수는 a × (d + g)로 계산합니다.

a가 d + g와 같으면 점수를 두 배로 합니다.

모든 참가자의 점수를 계산하여 최댓값을 출력합니다.

<br>

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    var n = int.Parse(Console.ReadLine()!);
    var best = 0;
    for (var i = 0; i < n; i++) {
      var p = Array.ConvertAll(Console.ReadLine()!.Split(), int.Parse);
      var a = p[0]; var d = p[1]; var g = p[2];
      var score = a * (d + g);
      if (a == d + g) score *= 2;
      if (score > best) best = score;
    }
    Console.WriteLine(best);
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int n; cin >> n;
  int best = 0;
  for (int i = 0; i < n; i++) {
    int a, d, g; cin >> a >> d >> g;
    int score = a * (d + g);
    if (a == d + g) score *= 2;
    if (score > best) best = score;
  }
  cout << best << "\n";

  return 0;
}
```
