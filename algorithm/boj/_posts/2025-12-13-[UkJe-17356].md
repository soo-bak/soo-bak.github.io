---
layout: single
title: "[백준 17356] 욱 제 (C#, C++) - soo:bak"
date: "2025-12-13 05:01:00 +0900"
description: 두 능력치 A,B로 M=(B−A)/400을 구해 욱이 이길 확률 1/(1+10^M)을 계산하는 백준 17356번 욱 제 문제의 C# 및 C++ 풀이와 해설
tags:
  - 백준
  - BOJ
  - 17356
  - C#
  - C++
  - 알고리즘
keywords: "백준 17356, 백준 17356번, BOJ 17356, UkJe, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[17356번 - 욱 제](https://www.acmicpc.net/problem/17356)

## 설명
두 능력치 A, B가 주어질 때 욱이 제를 이길 확률을 구하는 문제입니다.

<br>

## 접근법
M = (B - A) / 400으로 계산하고, 확률은 1 / (1 + 10^M)입니다.

공식을 그대로 실수로 계산하여 출력합니다.

<br>

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    var line = Console.ReadLine()!.Split();
    var a = int.Parse(line[0]);
    var b = int.Parse(line[1]);
    var m = (b - a) / 400.0;
    var ans = 1.0 / (1.0 + Math.Pow(10.0, m));
    Console.WriteLine($"{ans:F4}");
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  double a, b; cin >> a >> b;
  double m = (b - a) / 400.0;
  double ans = 1.0 / (1.0 + pow(10.0, m));
  cout.setf(ios::fixed);
  cout.precision(4);
  cout << ans << "\n";

  return 0;
}
```
