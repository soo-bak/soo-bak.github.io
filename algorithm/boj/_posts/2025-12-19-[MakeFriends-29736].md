---
layout: single
title: "[백준 29736] 브실이와 친구가 되고 싶어 🤸‍♀️ (C#, C++) - soo:bak"
date: "2025-12-19 22:47:00 +0900"
description: 친구 후보 구간 [A,B]와 조건 |K-점수|<=X가 겹치는 길이를 계산해 인원수를 구하는 백준 29736번 풀이
tags:
  - 백준
  - BOJ
  - 29736
  - C#
  - C++
  - 알고리즘
keywords: "백준 29736, 백준 29736번, BOJ 29736, MakeFriends, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[29736번 - 브실이와 친구가 되고 싶어 🤸‍♀️](https://www.acmicpc.net/problem/29736)

## 설명
점수가 a 이상 b 이하인 친구 후보 중, 브실이의 점수 k와의 차이가 x 이하인 사람 수를 구하는 문제입니다.

조건을 만족하는 사람이 없으면 IMPOSSIBLE을 출력합니다.

<br>

## 접근법
친구가 될 수 있는 점수 범위는 k - x 이상 k + x 이하입니다.

이 범위와 a 이상 b 이하의 교집합 길이가 답입니다.

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    var ab = Array.ConvertAll(Console.ReadLine()!.Split(), int.Parse);
    var kx = Array.ConvertAll(Console.ReadLine()!.Split(), int.Parse);
    
    var a = ab[0];
    var b = ab[1];
    var k = kx[0];
    var x = kx[1];
    var lo = Math.Max(a, k - x);
    var hi = Math.Min(b, k + x);
    var cnt = hi - lo + 1;

    if (cnt > 0) Console.WriteLine(cnt);
    else Console.WriteLine("IMPOSSIBLE");
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int a, b, k, x; cin >> a >> b >> k >> x;

  int lo = max(a, k - x);
  int hi = min(b, k + x);
  int cnt = hi - lo + 1;

  if (cnt > 0) cout << cnt << "\n";
  else cout << "IMPOSSIBLE\n";

  return 0;
}
```
