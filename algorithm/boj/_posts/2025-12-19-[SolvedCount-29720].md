---
layout: single
title: "[백준 29720] 그래서 님 푼 문제 수가? (C#, C++) - soo:bak"
date: "2025-12-19 22:47:00 +0900"
description: N 이상 되려면 M씩 K일 더 푼다는 말로부터 현재 풀이 수의 최소/최대를 역산하는 간단한 산술 풀이
tags:
  - 백준
  - BOJ
  - 29720
  - C#
  - C++
  - 알고리즘
  - 수학
  - arithmetic
keywords: "백준 29720, 백준 29720번, BOJ 29720, SolvedCount, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[29720번 - 그래서 님 푼 문제 수가?](https://www.acmicpc.net/problem/29720)

## 설명
n문제 이상을 달성하려면 하루 m문제씩 최소 k일 더 풀어야 할 때, 현재 푼 문제 수의 범위를 구하는 문제입니다.

<br>

## 접근법
현재 푼 문제 수의 최솟값은 n - m × k이고, 최댓값은 n - m × k + m - 1입니다.

최솟값이 음수가 될 수 있으므로 0으로 보정합니다.

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    var p = Array.ConvertAll(Console.ReadLine()!.Split(), int.Parse);
    var n = p[0];
    var m = p[1];
    var k = p[2];
    var minSolved = Math.Max(0, n - m * k);
    var maxSolved = n - m * k + m - 1;
    Console.WriteLine($"{minSolved} {maxSolved}");
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int n, m, k; cin >> n >> m >> k;
  int minSolved = max(0, n - m * k);
  int maxSolved = n - m * k + m - 1;
  cout << minSolved << " " << maxSolved << "\n";

  return 0;
}
```
