---
layout: single
title: "[백준 30655] Pique Esconde (C#, C++) - soo:bak"
date: "2025-12-21 09:00:00 +0900"
description: "백준 30655번 C#, C++ 풀이 - n명 중 찾아야 할 아이 두 명을 제외한 나머지 한 명을 찾는 문제"
tags:
  - 백준
  - BOJ
  - 30655
  - C#
  - C++
  - 알고리즘
keywords: "백준 30655, 백준 30655번, BOJ 30655, PiqueEsconde, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[30655번 - Pique Esconde](https://www.acmicpc.net/problem/30655)

## 설명
숨바꼭질에서 술래를 제외한 아이들 중 발견되지 않은 한 명의 번호를 찾는 문제입니다.

<br>

## 접근법
1부터 n까지의 합에서 술래 번호와 발견된 아이들의 번호를 모두 빼면 남은 한 명의 번호가 됩니다. 1부터 n까지의 합은 n × (n + 1) / 2로 계산할 수 있습니다.

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    while (true) {
      var first = Console.ReadLine()!.Split();
      var n = int.Parse(first[0]);
      var m = int.Parse(first[1]);
      if (n == 0 && m == 0) break;

      var missing = n * (n + 1) / 2 - m;
      for (var i = 0; i < n - 2; i++)
        missing -= int.Parse(Console.ReadLine()!);

      Console.WriteLine(missing);
    }
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int n, m;
  while (cin >> n >> m && (n || m)) {
    int missing = n * (n + 1) / 2 - m;
    for (int i = 0; i < n - 2; i++) {
      int x; cin >> x;
      missing -= x;
    }
    cout << missing << "\n";
  }

  return 0;
}
```
