---
layout: single
title: "[백준 14726] 신용카드 판별 (C#, C++) - soo:bak"
date: "2025-12-24 22:35:00 +0900"
description: "백준 14726번 C#, C++ 풀이 - Luhn 규칙으로 카드 번호의 유효성을 판별하는 문제"
tags:
  - 백준
  - BOJ
  - 14726
  - C#
  - C++
  - 알고리즘
  - 수학
  - 구현
  - 문자열
  - arithmetic
keywords: "백준 14726, 백준 14726번, BOJ 14726, CreditCard, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[14726번 - 신용카드 판별](https://www.acmicpc.net/problem/14726)

## 설명
16자리 카드 번호에 Luhn 공식을 적용해 유효성을 판단하는 문제입니다.

<br>

## 접근법
오른쪽부터 자리 인덱스를 세며 짝수 위치는 2배 처리합니다.

2배 결과가 10 이상이면 자릿수 합으로 바꾸고, 총합이 10으로 나뉘면 유효입니다.

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    var t = int.Parse(Console.ReadLine()!);
    for (var tc = 0; tc < t; tc++) {
      var s = Console.ReadLine()!;
      var sum = 0;
      var pos = 1;

      for (var i = s.Length - 1; i >= 0; i--, pos++) {
        var d = s[i] - '0';
        if (pos % 2 == 0) {
          d *= 2;
          if (d >= 10) d = d / 10 + d % 10;
        }
        sum += d;
      }

      Console.WriteLine(sum % 10 == 0 ? "T" : "F");
    }
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int t; cin >> t;
  while (t--) {
    string s; cin >> s;
    int sum = 0;
    int pos = 1;

    for (int i = (int)s.size() - 1; i >= 0; i--, pos++) {
      int d = s[i] - '0';
      if (pos % 2 == 0) {
        d *= 2;
        if (d >= 10) d = d / 10 + d % 10;
      }
      sum += d;
    }

    cout << (sum % 10 == 0 ? "T" : "F") << "\n";
  }

  return 0;
}
```
