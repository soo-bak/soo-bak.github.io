---
layout: single
title: "[백준 32068] 보물 찾기 (C#, C++) - soo:bak"
date: "2025-12-24 12:30:00 +0900"
description: "백준 32068번 C#, C++ 풀이 - S에서 좌우로 점점 멀어지는 위치를 검사할 때 처음 보물에 도달하는 단계 계산"
tags:
  - 백준
  - BOJ
  - 32068
  - C#
  - C++
  - 알고리즘
  - 수학
  - 구현
keywords: "백준 32068, 백준 32068번, BOJ 32068, TreasureHunt, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[32068번 - 보물 찾기](https://www.acmicpc.net/problem/32068)

## 설명
정해진 이동 규칙에 따라 위치를 조사할 때 보물에 처음 도달하는 단계를 구하는 문제입니다.

<br>

## 접근법
조사하는 위치는 시작점에서 오른쪽 1칸, 왼쪽 1칸, 오른쪽 2칸, 왼쪽 2칸 순으로 점점 멀어집니다.

따라서 오른쪽 보물과 왼쪽 보물에 도달하는 단계를 각각 계산하고, 더 빨리 도달하는 값을 출력합니다.

- - -

## Code

### C#
```csharp
using System;
using System.Text;

class Program {
  static void Main() {
    var t = int.Parse(Console.ReadLine()!);
    var sb = new StringBuilder();
    for (var i = 0; i < t; i++) {
      var parts = Console.ReadLine()!.Split();
      var l = int.Parse(parts[0]);
      var r = int.Parse(parts[1]);
      var s = int.Parse(parts[2]);

      var stepR = 2 * (r - s);
      var stepL = 2 * (s - l) + 1;
      sb.AppendLine(Math.Min(stepR, stepL).ToString());
    }
    Console.Write(sb.ToString());
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int t; cin >> t;
  while (t--) {
    int l, r, s; cin >> l >> r >> s;
    int stepR = 2 * (r - s);
    int stepL = 2 * (s - l) + 1;
    cout << min(stepR, stepL) << "\n";
  }

  return 0;
}
```
