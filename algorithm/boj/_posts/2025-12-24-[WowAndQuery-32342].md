---
layout: single
title: "[백준 32342] 와우와 쿼리 (C#, C++) - soo:bak"
date: "2025-12-24 12:30:00 +0900"
description: "백준 32342번 C#, C++ 풀이 - 문자열에서 WOW 부분 문자열 개수를 세는 문제"
tags:
  - 백준
  - BOJ
  - 32342
  - C#
  - C++
  - 알고리즘
keywords: "백준 32342, 백준 32342번, BOJ 32342, WowAndQuery, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[32342번 - 와우와 쿼리](https://www.acmicpc.net/problem/32342)

## 설명
각 문자열에서 WOW가 연속으로 등장하는 횟수를 구하는 문제입니다.

<br>

## 접근법
문자열을 처음부터 끝까지 순회하며 현재 위치에서 시작하는 길이 3의 부분 문자열을 확인합니다.

이후 해당 부분 문자열이 WOW와 일치하면 개수를 증가시키고, 최종 개수를 출력합니다.

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    var q = int.Parse(Console.ReadLine()!);
    for (var i = 0; i < q; i++) {
      var s = Console.ReadLine()!;
      var cnt = 0;
      for (var j = 0; j + 2 < s.Length; j++) {
        if (s[j] == 'W' && s[j + 1] == 'O' && s[j + 2] == 'W') cnt++;
      }
      Console.WriteLine(cnt);
    }
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int q; cin >> q;
  for (int i = 0; i < q; i++) {
    string s; cin >> s;
    int cnt = 0;
    for (int j = 0; j + 2 < (int)s.size(); j++) {
      if (s[j] == 'W' && s[j + 1] == 'O' && s[j + 2] == 'W') cnt++;
    }
    cout << cnt << "\n";
  }

  return 0;
}
```
