---
layout: single
title: "[백준 5919] Hay Bales (C#, C++) - soo:bak"
date: "2025-12-25 16:05:00 +0900"
description: 평균 높이까지 옮겨야 하는 건초 더미 수를 구하는 문제
---

## 문제 링크
[5919번 - Hay Bales](https://www.acmicpc.net/problem/5919)

## 설명
여러 더미의 높이가 주어질 때, 모두 같은 높이가 되도록 옮겨야 하는 건초 더미 수의 최소값을 구하는 문제입니다.

<br>

## 접근법
모든 더미의 합을 N으로 나눈 평균 높이를 구합니다.  
평균보다 큰 더미에서 초과한 개수만큼 옮기면 되므로, 초과분 합이 정답입니다.

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    var n = int.Parse(Console.ReadLine()!);
    var arr = new int[n];
    var sum = 0;
    for (var i = 0; i < n; i++) {
      arr[i] = int.Parse(Console.ReadLine()!);
      sum += arr[i];
    }

    var avg = sum / n;
    var moves = 0;
    for (var i = 0; i < n; i++)
      if (arr[i] > avg) moves += arr[i] - avg;

    Console.WriteLine(moves);
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int n; cin >> n;
  vector<int> a(n);
  int sum = 0;
  for (int i = 0; i < n; i++) {
    cin >> a[i];
    sum += a[i];
  }

  int avg = sum / n;
  int moves = 0;
  for (int i = 0; i < n; i++)
    if (a[i] > avg) moves += a[i] - avg;

  cout << moves << "\n";

  return 0;
}
```
