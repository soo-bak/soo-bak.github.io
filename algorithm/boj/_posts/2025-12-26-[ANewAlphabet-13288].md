---
layout: single
title: "[백준 13288] A New Alphabet (C#, C++) - soo:bak"
date: "2025-12-26 00:33:47 +0900"
description: 영문자를 새 알파벳 규칙으로 치환해 출력하는 문제
---

## 문제 링크
[13288번 - A New Alphabet](https://www.acmicpc.net/problem/13288)

## 설명
문장 속 영문자를 정해진 새 알파벳 규칙으로 바꾸고 나머지는 그대로 출력하는 문제입니다.

<br>

## 접근법
먼저 알파벳 소문자에 대한 치환표를 준비합니다.

다음으로 문자열을 한 글자씩 보면서 알파벳이면 대응 문자열로 바꿉니다.

이후 알파벳이 아닌 문자는 그대로 유지합니다.

마지막으로 변환된 결과를 출력합니다.

<br>

- - -

## Code

### C#
```csharp
using System;
using System.Text;

class Program {
  static void Main() {
    var map = new[] {
      "@", "8", "(", "|)", "3", "#", "6", "[-]", "|", "_|", "|<", "1",
      "[]\\/[]", "[]\\[]", "0", "|D", "(,)", "|Z", "$", "']['", "|_|", "\\/",
      "\\/\\/", "}{", "`/", "2"
    };

    var s = Console.ReadLine()!;
    var sb = new StringBuilder();
    for (var i = 0; i < s.Length; i++) {
      var c = s[i];
      if (c >= 'A' && c <= 'Z') c = (char)(c + 32);
      if (c >= 'a' && c <= 'z') sb.Append(map[c - 'a']);
      else sb.Append(c);
    }

    Console.WriteLine(sb.ToString());
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  vector<string> map = {
    "@", "8", "(", "|)", "3", "#", "6", "[-]", "|", "_|", "|<", "1",
    "[]\\/[]", "[]\\[]", "0", "|D", "(,)", "|Z", "$", "']['", "|_|", "\\/",
    "\\/\\/", "}{", "`/", "2"
  };

  string s;
  getline(cin, s);

  string out;
  out.reserve(s.size());
  for (char c : s) {
    if (c >= 'A' && c <= 'Z') c = char(c + 32);
    if (c >= 'a' && c <= 'z') out += map[c - 'a'];
    else out += c;
  }

  cout << out << "\n";
  return 0;
}
```
