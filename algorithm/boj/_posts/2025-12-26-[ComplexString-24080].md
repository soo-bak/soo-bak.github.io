---
layout: single
title: "[백준 24080] 複雑な文字列 (Complex String) (C#, C++) - soo:bak"
date: "2025-12-26 01:11:56 +0900"
description: 문자열에 등장하는 서로 다른 문자가 3종 이상인지 판별하는 문제
---

## 문제 링크
[24080번 - 複雑な文字列 (Complex String)](https://www.acmicpc.net/problem/24080)

## 설명
문자열에 등장하는 서로 다른 문자가 3종 이상인지 판별하는 문제입니다.

<br>

## 접근법
먼저 문자열에서 등장한 문자를 표시합니다.

다음으로 서로 다른 문자 개수를 셉니다.

마지막으로 개수가 3 이상이면 Yes, 아니면 No를 출력합니다.

<br>

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    var n = int.Parse(Console.ReadLine()!);
    var s = Console.ReadLine()!;

    var seen = new bool[5];
    for (var i = 0; i < s.Length; i++) {
      seen[s[i] - 'A'] = true;
    }

    var cnt = 0;
    for (var i = 0; i < 5; i++) {
      if (seen[i]) cnt++;
    }

    Console.WriteLine(cnt >= 3 ? "Yes" : "No");
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int n; cin >> n;
  string s; cin >> s;

  bool seen[5] = {};
  for (char c : s) {
    seen[c - 'A'] = true;
  }

  int cnt = 0;
  for (int i = 0; i < 5; i++) {
    if (seen[i]) cnt++;
  }

  cout << (cnt >= 3 ? "Yes" : "No") << "\n";
  return 0;
}
```
