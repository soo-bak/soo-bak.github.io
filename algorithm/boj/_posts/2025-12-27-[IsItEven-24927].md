---
layout: single
title: "[백준 24927] Is It Even? (C#, C++) - soo:bak"
date: "2025-12-27 05:15:00 +0900"
description: "백준 24927번 C#, C++ 풀이 - 수열 곱이 2^K로 나누어떨어지는지 판별하는 문제"
tags:
  - 백준
  - BOJ
  - 24927
  - C#
  - C++
  - 알고리즘
keywords: "백준 24927, 백준 24927번, BOJ 24927, IsItEven, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[24927번 - Is It Even?](https://www.acmicpc.net/problem/24927)

## 설명
N개의 수가 주어질 때, 곱이 2^K로 나누어떨어지는지 확인하는 문제입니다.

모든 수를 곱하지 않고, 각 수의 2의 지수 합이 K 이상인지로 판별할 수 있습니다.

<br>

## 접근법
각 수에서 2로 몇 번 나눌 수 있는지 세어 합산합니다. 합이 K 이상이면 1, 아니면 0을 출력합니다.

<br>

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    var first = Console.ReadLine()!.Split();
    var n = int.Parse(first[0]);
    var k = int.Parse(first[1]);

    long two = 0;
    for (var i = 0; i < n; i++) {
      var x = long.Parse(Console.ReadLine()!);
      while ((x & 1) == 0) {
        two++;
        x >>= 1;
      }
    }

    Console.WriteLine(two >= k ? 1 : 0);
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int n, k; cin >> n >> k;
  ll two = 0;
  for (int i = 0; i < n; i++) {
    ll x; cin >> x;
    while ((x & 1LL) == 0) {
      two++;
      x >>= 1;
    }
  }

  cout << (two >= k ? 1 : 0) << "\n";

  return 0;
}
```
