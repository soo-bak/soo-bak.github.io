---
layout: single
title: "[백준 20341] Moderate Pace (C#, C++) - soo:bak"
date: "2025-12-27 13:45:00 +0900"
description: "백준 20341번 C#, C++ 풀이 - 하루별 세 사람의 거리 제안에서 중앙값을 선택해 계획을 만드는 문제"
tags:
  - 백준
  - BOJ
  - 20341
  - C#
  - C++
  - 알고리즘
keywords: "백준 20341, 백준 20341번, BOJ 20341, ModeratePace, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[20341번 - Moderate Pace](https://www.acmicpc.net/problem/20341)

## 설명
매일 세 사람이 제시한 러닝 거리가 주어질 때, 각 날마다 세 값의 중앙값을 선택해 일별 계획을 출력하는 문제입니다.

<br>

## 접근법
각 날의 세 값을 정렬해 두 번째 값을 선택합니다.

모든 날에 대해 이를 반복하여 출력합니다.

<br>

## Code

### C#
```csharp
using System;
using System.Linq;
using System.Text;

class Program {
  static void Main() {
    var n = int.Parse(Console.ReadLine()!);
    var k = Console.ReadLine()!.Split().Select(int.Parse).ToArray();
    var a = Console.ReadLine()!.Split().Select(int.Parse).ToArray();
    var b = Console.ReadLine()!.Split().Select(int.Parse).ToArray();

    var sb = new StringBuilder();
    for (var i = 0; i < n; i++) {
      var arr = new int[] { k[i], a[i], b[i] };
      Array.Sort(arr);
      sb.Append(arr[1]);
      if (i + 1 < n) sb.Append(' ');
    }
    Console.WriteLine(sb.ToString());
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef vector<ll> vll;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int n; cin >> n;
  vll k(n), a(n), b(n);
  for (int i = 0; i < n; i++)
    cin >> k[i];
  for (int i = 0; i < n; i++)
    cin >> a[i];
  for (int i = 0; i < n; i++)
    cin >> b[i];

  for (int i = 0; i < n; i++) {
    array<ll, 3> v = {k[i], a[i], b[i]};
    sort(v.begin(), v.end());
    cout << v[1] << (i + 1 == n ? '\n' : ' ');
  }

  return 0;
}
```
