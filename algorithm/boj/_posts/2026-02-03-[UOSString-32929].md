---
layout: single
title: "[백준 32929] UOS 문자열 (C#, C++) - soo:bak"
date: "2026-02-03 21:33:00 +0900"
description: "백준 32929번 C#, C++ 풀이 - 무한 반복되는 UOS 문자열의 x번째 문자 찾기"
tags:
  - 백준
  - BOJ
  - 32929
  - C#
  - C++
  - 알고리즘
  - 구현
keywords: "백준 32929, 백준 32929번, BOJ 32929, UOS 문자열, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[32929번 - UOS 문자열](https://www.acmicpc.net/problem/32929)

## 설명
UOS를 무한히 반복한 문자열에서 x번째 문자를 구하는 문제입니다. 패턴 길이가 3이므로 (x-1) mod 3에 따라 U, O, S 중 하나가 됩니다.

<br>

## 접근법
idx = (x - 1) % 3 값을 계산해 0이면 U, 1이면 O, 2이면 S를 출력합니다.

<br>

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    long x = long.Parse(Console.ReadLine()!);
    long idx = (x - 1) % 3;
    char c = idx == 0 ? 'U' : idx == 1 ? 'O' : 'S';
    Console.WriteLine(c);
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  ll x; cin >> x;
  ll idx = (x - 1) % 3;
  char c = (idx == 0) ? 'U' : (idx == 1) ? 'O' : 'S';
  cout << c << "\n";
  return 0;
}
```
