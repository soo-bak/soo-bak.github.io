---
layout: single
title: "[백준 30162] Filling Shapes (C#, C++) - soo:bak"
date: "2026-04-20 22:22:00 +0900"
description: "백준 30162번 C#, C++ 풀이 - 홀수는 불가능하고 짝수 길이는 2^(n/2)로 계산하는 문제"
tags:
  - 백준
  - BOJ
  - 30162
  - C#
  - C++
  - 알고리즘
  - DP
keywords: "백준 30162, 백준 30162번, BOJ 30162, Filling Shapes, C# 풀이, C++ 풀이, 알고리즘"
---

## 문제 링크
[30162번 - Filling Shapes](https://www.acmicpc.net/problem/30162)

## 설명
주어진 모양으로 `3 × n` 보드를 빈칸 없이 채우는 방법의 수를 구하는 문제입니다.

<br>

## 접근법
이 도형으로 `3 × n` 보드를 채우려면, 열을 항상 두 칸씩 묶어서 생각하게 됩니다.

따라서 `n`이 홀수이면 애초에 채울 수 없어서 답은 `0`입니다. 반대로 `n`이 짝수라면, 길이 `2`를 하나 늘릴 때마다 이전 경우마다 선택이 두 가지씩 생기므로 전체 경우의 수는 `2^(n/2)`가 됩니다.

즉 정답은 홀수일 때 `0`, 짝수일 때 `2^(n/2)`입니다.

<br>

- - -

## Code

### C#
```csharp
using System;

class Program {
  static void Main() {
    int n = int.Parse(Console.ReadLine()!);

    if (n % 2 == 1) {
      Console.WriteLine(0);
      return;
    }

    Console.WriteLine(1L << (n / 2));
  }
}
```

### C++
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int n;
  cin >> n;

  if (n % 2 == 1) {
    cout << 0 << "\n";
    return 0;
  }

  cout << (1LL << (n / 2)) << "\n";

  return 0;
}
```
